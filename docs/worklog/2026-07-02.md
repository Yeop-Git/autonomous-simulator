# Worklog — 2026-07-02

> 매일 마무리 로그. 형식: **오늘 한 일 → 현재 상태 → 다음날 착수 지점**.

## 오늘 한 일

### Phase 7 — 경로탐색 알고리즘 비교 (A*/RRT/RRT*) 완성
- **RRT** (`server/planners/rrt.py`): `_rrt_common.py`의 sample/steer/collision_free/reconstruct 사용.
  연속 xz 공간에서 랜덤 트리 성장, `world.is_blocked`로 장애물 회피. 첫 목표 연결 시 종료.
  시드 고정 → 결정적. `last_nodes/last_iters/last_cost` 기록.
- **RRT\*** (`server/planners/rrt_star.py`): choose-parent + rewire(반경 12m)로 경로 품질 개선.
  전체 반복예산을 소진하며 정제 후, 목표 근방 최소비용 노드로 경로 복원.
- `planners/__init__.py`에 `RRTPlanner`/`RRTStarPlanner`/`RRTConfig` export.
- `_rrt_common.py`에 `polyline_length` 추가.
- **테스트** `tests/test_rrt.py` (13개): 자유공간 직선, 벽 틈새 우회(충돌프리 검증), 도달불가→빈경로,
  목표점 차단→빈경로, 시드 결정성, RRT\* 품질(≤RRT×1.05), 인스펙션 필드. → 전체 **105 passed**.

### 실험 + 로깅 + 차트 + 보고서 (plan §20.1, §21.3)
- **`experiments/run_algorithm_compare.py`**: 3개 시나리오(`road_open` 도로그래프 / `road_detour` 차선 차단
  / `obstacle_field` 비정형 장애물) × 3 플래너 × 5 시드 × {1,5,20}대. 계산시간/경로길이/성공률/노드수 →
  `results/algo_compare_raw.csv`(1170행) + `results/algo_compare_summary.csv`(27행, 다중시드 평균±std).
- **`experiments/make_charts.py`**(headless Agg) + **`experiments/analysis.ipynb`**(nbclient로 실행검증 완료):
  §21.3 차트 → `results/charts/{algo_compare,algo_time_vs_vehicles,lka_lateral_error}.png`.
- **`docs/experiment_results.md`**: §20.1(A*/RRT/RRT*) + §20.2(LKA) 결과·해석. §20.3–20.5는 로직 완료·씬 대기 표기.

### 핵심 결과
- **도로그래프**: A* 우세 — 도로규칙 준수 경로(233.8m, ~0.35ms). RRT 164m는 격자를 가로지르는 비합법 직선.
- **차단/비정형 장애물**: A* **실패(0%)** — 차선그래프에 대체 엣지 없음. RRT/RRT*만 우회 성공.
- **RRT vs RRT\***: RRT ~1ms 들쭉날쭉(~113m), RRT\* ~97m로 ~14% 단축하지만 **~700× 느림(~0.5–0.7s)** → 실시간 부적합.
- **정책 권고**: 전역 도로경로=A*, 온라인 장애물 우회=RRT, 품질 벤치마크=RRT\*.

## 현재 상태
- 테스트: `cd server && python -m pytest -q` → **105 passed**
- 실험 재현: `python experiments/run_algorithm_compare.py && python experiments/run_lka_test.py && python experiments/make_charts.py`
- Phase 0–7 서버측 전부 완료. 남은 것은 **사람(Unity 에디터) 작업**뿐.
- 미커밋(작업트리에 신규 파일 다수). 커밋/푸시는 형님 지시 대기.

## 사람(형님) 작업 대기 — Unity 에디터
- 씬 구성(직선/곡선 트랙, 고속도로 본선+램프, 시내 교차로/횡단보도) → 차선 export.
- §20.3(합류)/§20.4(교차로)/§20.5(돌발) 실험은 위 씬이 있어야 지표 CSV 산출 가능(서버 로직은 완료).

---

## 🚀 내일 착수 지점
1. (형님 결정) Phase 7 산출물 커밋 + 필요 시 main 병합/PR.
2. Unity 씬 준비되면 §20.3–20.5 실험을 헤드리스 축소판으로도 돌릴 수 있게
   `server/scenarios/networks.py`에 합성 씬(램프/교차로) 추가 → `run_*` 러너 작성.
3. (선택) 교차로 예약·합류를 라이브 `central_control` FSM에 통합.

> 시작 명령: `cd server && python -m pytest -q` 로 105 그린 확인.
