# Worklog — 2026-06-29

> 매일 마무리 로그. 형식: **오늘 한 일 → 현재 상태 → 다음날 착수 지점**.
> 다음날은 이 파일 맨 아래 "내일 착수 지점"부터 보면 됩니다.

## 오늘 한 일

### 서버 구현 (Phase 0–6, 테스트 92개 통과)
- **Phase 0/2 (수직 슬라이스 서버측)**: `world_model.py`(차선 그래프 World 프로토콜 + 동적 월드),
  `planners/astar.py`(차선 A* + 중심선 스티칭), `central_control.py`(틱 파이프라인),
  `main.py`(WebSocket + 동기화 검사), `tools/fake_unity_client.py`. 라이브 슬라이스 동작 확인.
- **Phase 3**: `collision_predictor.py`(해석적 TTC), `behavior.py`(FSM+선행차), `controllers/acc.py`,
  `headless_sim.py`(Unity 없는 다중차량 테스트벳).
- **Phase 4**: `controllers/lateral.py`(Pure Pursuit/Stanley/PID + frenet 오차),
  `logging_csv.py`(고정 스키마, 헤드리스에 연동), `noise.py`(Full/Noisy/Local),
  `experiments/run_lka_test.py`(40/60/80/100 스윕 → CSV).
- **Phase 5 (고속도로)**: `lane_change.py`(gap 수용), `merge.py`(합류 예약),
  `emergency.py`(긴급차 양보, 컨트롤러 연동), `metrics.py`(지표). 돌발이벤트→장애물+재계획.
- **Phase 6 (시내)**: `traffic.py`(신호등), `intersection.py`(예약형 무신호 교차로),
  보행자 정지(충돌예측), 공유 지표.

### 품질 점검 (반복점검)
- 독립 리뷰 에이전트 3개로 적대적 검증 → **버그 9개 수정**, 전부 회귀 테스트(`tests/test_review_fixes.py`)로 고정.
  (충돌 샘플링 터널링, 선행차 세그먼트 누락, urban_grid 텔레포트 엣지, A* 단일차선 목표 누락,
  경로 캐시 이탈 무효화, 헤드리스 투영 점프, Unity 동시전송 가드, MovingObject null vec3 등)
- 추가 개선: 정상 추종을 Stopping이 아닌 Following으로 분리(긴급 제동 안전망은 유지),
  목표 감속을 경로따라 거리로 계산.

### Unity 6 (6000.0.42f1) 클라이언트 스캐폴드
- `V2XClient`(Newtonsoft + 동시전송 가드), `Lane`/`RoadNetworkManager`,
  `VehicleController`+`LKAController`, `SimulationManager`, `PathVisualizer`,
  `DebugDashboard`, `Editor/LaneNetworkExporter`. `Packages/manifest.json` 세팅.
- 가이드: `docs/unity_setup.md`.

## 현재 상태
- 브랜치: **`feat/central-server-phases-0-6`** (origin 푸시 완료, PR 미생성)
- 테스트: `cd server && python -m pytest -q` → **92 passed**
- 라이브 슬라이스: `python main.py` + `python tools/fake_unity_client.py` → `DONE OK`
- 미완: **Phase 7(RRT/RRT*)** — 토대 `planners/_rrt_common.py`만 존재(어디에도 import 안 됨).

## 사람(형님) 작업 대기 — Unity 에디터
- 씬에 차선/waypoint 배치 → `V2X ▸ Export Lane Network` → `server/scenarios/<scene>_lanes.json` 저장.
- 차량 프리팹 + `SimulationManager`/`V2XClient` 배선(같은 SimulationManager를 provider·sink 양쪽에).
- 절차: `docs/unity_setup.md §4`.

---

## 🚀 내일 착수 지점 (우선순위 순)

1. **Phase 7 — RRT 구현** (착수 파일: `server/planners/rrt.py`)
   - `_rrt_common.py`의 `sample/steer/collision_free/reconstruct` 사용해 `RRTPlanner.plan(start, goal, world)` 작성.
   - `planners/__init__.py`에 export, `tests/test_rrt.py` 추가(차단 디스크 우회 경로 검증, 시드 고정).
2. **Phase 7 — RRT\*** (`server/planners/rrt_star.py`): 근방 재배선(rewire)로 경로 품질 개선.
3. **알고리즘 비교 실험** (`experiments/run_algorithm_compare.py`):
   A* vs RRT vs RRT* — 계산시간/경로길이/성공률, 다중 시드. 결과 `experiments/results/`(gitignored) + 요약.
4. **`docs/experiment_results.md`** 초안 + (선택) `experiments/analysis.ipynb` 차트.
5. (선택) 교차로 예약·합류를 라이브 `central_control` FSM에 통합(현재는 모듈+단위테스트).

> 시작 명령: `cd server && python -m pytest -q` 로 그린 확인 후 1번부터.
